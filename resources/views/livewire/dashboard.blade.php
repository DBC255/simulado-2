<div>
    <h2>Bem vindo qual a ação sera realizada</h2>
     <a class="btn btn-secondary" href="{{ route('produto.index') }}">Produtos</a>
     <br>
     <a class="btn btn-secondary" href="{{ route('categoria.index') }}">categorias</a>
     <br>
     <a class="btn btn-secondary" href="{{ route('movimentacao.index') }}">movimentacoes</a>

    {{-- If you look to others for fulfillment, you will never truly be fulfilled. --}}
</div>
